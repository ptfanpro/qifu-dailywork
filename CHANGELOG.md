# Changelog

## 9.5.83 - 2026-08-29

- 修复“高票 OCR 结果或纯数字文件名被当成天然正确”的安全缺口：清晰编号必须同时满足多裁框 OCR 共识和当天 PDF 唯一编号索引；折叠、遮挡以及缺号/顺序推断结果必须再由对应 PDF 页面正文指纹确认，冲突、不唯一或相似度不足时立即停止。
- 现有纯数字照片也会重新读取纸面可见编号并与文件名、PDF 页面三方核对，避免错误编号因文件已经命名而永久绕过识别流程。
- 界面新增“重新核对编号”只读入口，会生成完整诊断但不改名、不压缩、不上传、不修改平台；可在修复或迁移版本后先复核再决定是否继续。
- 新增清晰编号双证据确认、PDF 指纹反证错误推断、数字文件名与纸面 OCR 冲突、旧数字照片暂不可读但不误报四类永久回归，防止后续版本再次退回单一 OCR 证据或制造假冲突。

## 9.5.82 - 2026-08-29

- 修复 8 月 28 日相机构图中纸色与木架、神像连通后，动态纸框几乎覆盖整幅照片，导致 599–602 清晰编号全部裁错或带入花边的问题；新增上下两组微型滑动编号带，并以未二值化红通道及多阈值形成同号共识。
- 本地 PDF 页面指纹新增当前 4:3 相机的供灯/供水固定纸面候选；供水 PDF 同样纳入红纸候选，错误纸框把横版误报为竖版时回退全部未认领页面，仍保留相似度、次优差距和一页一图约束。
- 修复低曝光第二张供灯场景和黄色台阶填充率略低的供水全景未进入场景分类的问题；真实 8 月 28 日只读回放已确认 599、600、601、602、2.2、2.5，最终仅保留实际缺少的 597、598。
- 版本历史以 GitHub V9.5.80 分支为祖先，补录并标记本机 V9.5.81 人工验证码回退差异后再发布本版，避免继续从散落目录复制修复。

## 9.5.81 - 2026-08-28

- 按用户要求将桌面日常业务回退为人工验证码登录：移除机器接口配置、检查和清除入口，初始化及照片/PDF执行只复用 Edge 登录会话。
- 继续允许账号密码以 Windows 当前用户 DPAPI 加密保存；检测到验证码字段（包括真实页面 `#code`）时绝不自动提交，等待本人输入并点击登录后自动续跑。
- 保留 V9.5.76 照片编号与场景识别、V9.5.78 上传接口精确回执，以及此前固定日期、断点、防重、分页和状态闭环修复。
- 新增界面无机器入口、人工验证码提示和版本标题回归，防止后续改版重新把免验证码通道混入日常入口。

## 9.5.80 - 2026-08-28

- 接入受限机器接口的第一项日常业务能力：按界面所选照片业务日期读取七类日清单，不打开后台登录页，不触发验证码，也不创建管理员 Session。
- 客户端强制要求 `daily:plan:read`、`dryRun=true`、`Asia/Shanghai` 和原业务日期完全一致；逐组校验数量、订单标识、上传布尔状态及 SHA-256，任何契约漂移都会安全停止。
- 机器接口检查成功后只在本机非同步目录保存业务日期、七类数量和匿名订单集合哈希；不保存客户姓名、祈福正文、照片、账号或令牌。
- 高级工具的“检查接口/日清单”同时验证机器身份和所选照片日期清单；当前版本仍不切换上传、PDF 或状态写入，待服务端隔离部署与写接口逐项验收后再启用。
- 新增日清单日期非法、权限缺失、响应日期错配、分组/汇总结构与密钥不泄漏回归；保留 V9.5.79 及此前全部照片、PDF、分页、断点和固定日期测试。

## 9.5.79 - 2026-08-28

- 把迁移任务提交 `efdebba` 中的机器身份客户端正式接入桌面界面：可在高级工具中配置、检查和清除机器接口凭据。
- 机器接口地址、客户端编号和机器密钥使用 Windows 当前用户 DPAPI 单独加密，保存于本机非同步 `secure-automation.dat`，与后台人员账号密码完全分离。
- 将原“自动登录”明确更名为“浏览器辅助登录”：无验证码时可自动提交，出现验证码时仍由本人输入；机器接口认证不会创建后台人员 Session，也不会绕过验证码。
- 当前服务端仅实现且尚未生产部署 `/internal/automation/v1/token` 和 `/status`，因此接口检查失败不阻断照片/PDF现有浏览器流程。业务接口开放并完成隔离测试前，不假装已实现全自动后台登录。
- 新增机器凭据明文泄漏、DPAPI 往返、界面入口和认证分流回归；不改 V9.5.78 上传回执，以及此前照片、PDF、分页、断点和固定日期规则。

## 9.5.78 - 2026-08-28

- 修复福单图实际上传完成后，程序把月份控件内隐藏的 `laydate` 脚本误当成上传结果，等待一分钟后仍报“没有返回本批上传数量”的问题。
- 上传前直接监听平台 `uploadPic/name` AJAX 响应，并只从明确的 `result.message`、`uploadedCount` 或 `count` 回执字段确认本批精确数量；不从订单 ID、累计数、页面脚本或隐藏 DOM 猜测上传结果。
- 提交前把本批文件名和开始时间原子写入断点回执；即使浏览器或网络在服务器接收后中断，后续也能明确识别待对账批次，不把整日照片重新上传。
- Layui 后备消息采集由 `textContent` 改为可见 `innerText`，保留原生数字弹窗与明确消息层后备，并新增接口 JSON、脚本污染和错误累计数永久回归。
- 保留 V9.5.77 机器认证客户端基础及 V9.5.76 以前全部编号、场景、固定日期、断点续跑、部分上传、分页和安全确认回归。

## 9.5.77 - 2026-08-28

- 新增与后台 `/internal/automation/v1/token`、`/status` 对接的 HMAC-SHA256 机器认证客户端，只取得短期受限令牌，不建立后台人员 Session，也不识别或绕过验证码。
- 机器密钥使用 Windows 当前用户 DPAPI 单独加密保存，不与后台人员账号密码共用；请求只允许 HTTPS（本机测试除外），密钥不写入命令行、环境变量或日志。
- 新增 `automation-auth-check` 本地自检动作及固定时间、固定随机数、令牌状态、非 HTTPS 拒绝、明文泄漏和 DPAPI 桌面往返回归。
- 该基础能力尚未替换现有浏览器业务动作；查询、上传、PDF 和状态接口完成并通过隔离验收前，现行人工验证码登录仍保留。

## 9.5.76 - 2026-08-28

- 修复照片右上角编号被分隔符、空格或 OCR 字母误拆时无法归属的问题；Windows 本地 OCR 会保留非空原始文本，并仅在当天 PDF 编号集合中重组候选。
- 对清晰编号裁框增加同一区域 `AUTO` 模式复核；只有两个独立 OCR 证据一致或其他唯一强证据成立时才自动编号，冲突时继续留给人工。
- PDF 尾部连续页面即使单页编号未读出，也可由已确认的连续编号边界安全补全索引；仍禁止越出当天 PDF 编号集合。
- 扩充当前相机纸张构图与供水、供灯场景结构回归，避免把场景图送入福单编号流程。
- 2026-08-27 生产目录只读回放：54 个 PDF 编号中正确归属 53 张现有福单及 3 张场景图，只报告当天真实未回传的 `556.jpg`；未修改 NAS 或线上状态。
- 保留 V9.5.75 验证码人工接管及此前全部固定日期、断点续跑、部分上传、分页、场景范围和安全确认规则。

## 9.5.75 - 2026-08-28

- 修复平台验证码输入框使用 `id="code"` 时未被识别，导致程序以空验证码自动提交的问题。
- 自动登录改为验证码人工接管：账号密码自动安全填入，验证码由本人输入并点击登录，成功后原任务自动继续。
- 配置加密凭据时登录等待上限改为 10 分钟，并新增真实登录字段回归测试。
- 保留 V9.5.74 及此前全部回归，不改照片、PDF、分页、断点续跑和固定默认日期规则。

## 9.5.74 - 2026-08-27

- 新增 Windows 当前用户 DPAPI 加密凭据存储及界面化设置/清除入口，明文不写入文件、日志、命令行或环境变量。
- 登录页检测后仅自动提交一次；验证码、凭据错误或页面结构异常时安全停止，防止循环重试和账号锁定。
- 已有专用 Edge 登录状态继续直接复用；未配置凭据时保留人工登录路径。
- 凭据固定保存在本机非同步运行目录，并由 Git 忽略；迁移到新电脑或新 Windows 用户后必须重新录入。
- 新增 DPAPI 桌面往返、明文泄漏、清除、自动登录单次尝试和敏感参数防回归测试，保留 V9.5.73 及此前全部回归。

## 9.5.73 - 2026-08-27

- 修复纸张编号靠近上边缘时，窄裁框分辨率不足导致肉眼清晰但本地 OCR 漏读的问题；编号裁框改为纸张相对坐标并以 1600 像素宽预处理。
- 扩展供水、供灯场景的结构特征，兼容紧凑暖色灯阵和下半画面主体构图，避免把场景图误列为未编号福单。
- 本地 PDF 页面形状匹配会排除已由文件名或可靠 OCR 占用的编号，防止疑难照片抢占已经确认的页面。
- 新增纸张上沿编号、场景结构和已占编号排除的永久回归测试；2026-08-26 生产目录只读回放中，529、531、532 及三张场景图全部正确归类，仅保留真实缺失的 530。
- 保留 V9.5.72 的历史零待办闭环判断，以及照片默认昨天、PDF 默认今天等既有规则。

## 9.5.72 - 2026-08-26

- 修复旧版本已完成日期因缺少 `order-manifest.json` 被反复列为历史未闭环的问题。
- 新增旧版完整 PDF/照片对应凭据判定；四项线上待办均为 0 时补记本地闭环回执。
- 增加正反回归测试，继续锁定“照片默认昨天、PDF 默认今天、历史待办独立显示”。

## 9.5.71 - 2026-08-26

- 固定软件启动日期：照片业务日期为北京时间昨天，PDF 业务日期为北京时间今天。
- 删除上次手动日期和最近运行日期覆盖启动默认值的旧逻辑；手动切换仍可正常初始化所选日期，但不跨重启保存。
- 历史未闭环日期保持独立扫描、独立选择和处理完成后恢复上方日期，不再参与启动默认值决策。
- 新增动态北京时间启动断言和源码防回归断言，确保旧设置字段与最近运行回退逻辑不能重新引入。

## 9.5.70 - 2026-08-26

- 修复单次只补 1 张场景图时，在进入视觉分类前被“至少两张候选”旧门槛直接拒绝的问题。
- 单张清晰供水/供灯图使用绝对视觉证据独立归类；视觉证据不足且两类位置均空时仍安全留给人工，不猜测业务类别。
- 当 `2.1/2.2` 或 `2.5/2.6` 已被确认场景占满时，新增候选按唯一剩余业务槽位续接，避免要求重复发送已经上传的场景图。
- 新增单张供水、单张供灯、单张模糊图、唯一剩余槽位四组永久回归；V9.5.64–V9.5.69 的编号、部分上传、断点、分页和确认竞态测试全部保留。
- 使用 2026-08-25 生产目录完成只读全目录回放：32 张已命名福单保持不动，仅把新增清晰供水原图规划为 `2.5.jpg`，无缺号、冲突或待人工项。

## 9.5.69 - 2026-08-26

- 修复福单图月份提交后的确认层竞态：Layui 动画或原生 dialog 已关闭按钮时，不再由动态 Locator 等待默认 30 秒。
- `scheduleSiteClick` 改用短时一次性元素句柄；业务按钮仍严格报错，瞬时确认按钮允许有界消失并继续读取明确结果。
- 新增确认按钮消失与业务按钮缺失的正反回归，保留 V9.5.64–V9.5.68 全部历史测试。
- V9.5.68 本次真实断点保留 25 张逐文件回执，7 张补图停在月份提交后；新版本续跑前必须先只读线上对账，不盲目重传。

## 9.5.68 - 2026-08-26

- 修复新一批 4:3 回传照片的固定编号裁框偏高问题，将 temple 编号带覆盖到画面高度约 50%–53%。
- 收紧假竖版判断：木架、桌布与纸张连色形成的高连通域不再单独决定只使用 portrait 构图。
- 每张疑难照片有限复核三种当前相机编号行，并让 Windows 本地 OCR 同时读取两种最可能构图；仍只接受当天 PDF 编号集合中的唯一结果。
- 新增供水全景“全宽金色台阶”结构分类，防止被大面积黄色连通域误报为福单纸张。
- 新增两组脱敏回归，并用 2026-08-25 的 8 张真实补图只读重放：场景 1 张，福单 496–502 共 7 张全部闭合。

## 9.5.67 - 2026-08-26

- 修复部分回传批次的场景图需求作用域：旧逻辑只要目录中存在供水 PDF 就要求供水场景图，即使所有供水福单都尚未回传，也会阻断已齐的供灯订单。
- 使用 `photo-prepare-plan.json` 中完整的 PDF 编号归属索引，把当前纯数字福单准确归类为供水、供灯或牌位，只检查当前可上传订单真正需要的场景类别。
- 保留索引不完整时的整日 PDF 保守校验，避免旧断点或异常索引导致漏传场景。
- 新增正反两组回归：供水福单全部缺失时不要求供水场景；任一供水福单到达后仍必须有供水场景。
- 2026-08-25 生产目录只读验证通过：25 张现有福单仅需供灯场景，7 张缺失的 496–502 均属于供水，未修改 NAS 和线上状态。

## 9.5.66 - 2026-08-26

- 修复近景供灯照片中的金色灯架连成大块后，被纸色检测误判为“大张黄纸”并送入福单 OCR 的问题。
- 场景分类新增严格的全画面结构证据：只有无矩形纸张、连通区域接近全宽、文字边缘极少且画面高度均匀时，才能越过纸张色块兜底进入场景分支。
- 修复补图只包含同一场景类别时无法自动归类的问题；两张都独立满足强供灯特征时直接按顺序命名 `2.1.jpg`、`2.2.jpg`，不会强拆成一灯一水，缺少供水图仍单独待补。
- 为 2026-08-25 生产故障加入脱敏结构指标和同类双灯合成图回归测试，同时保留真实纸张、浅色场景、编号消歧、上传、分页、断点与跨盘安全测试。
- 对两张真实原图完成只读专项复核：均判定为供灯场景且分配结果唯一；没有修改 NAS 原图或线上状态。

## 9.5.65 - 2026-08-26

- 修复增量补图时相邻锚点已经改成数字文件、导致清晰的 `513` 被低置信度 OCR 误读为第二张 `503` 后无法自动归位的问题。
- 重号消歧现在统一使用 PDF 唯一编号集、已有数字文件占用集合和候选证据强弱；只在唯一解成立时纠正，避免覆盖真实高置信度编号。
- 将安全的单字符混淆从 `6/8` 扩展到受严格约束的 `0/1`，新增低置信度修复与高置信度拒绝两组回归测试。
- 使用 2026-08-25 真实业务目录完成只读重放：`503/513` 闭合、重复项为 0、真实未回传编号为 7 个，NAS 原图未被修改。
- 版本号、窗口标题、README、变更记录和测试断言同步更新，后续发布从 Git 标签生成，禁止从旧复制目录覆盖修复。

## 9.5.64 - 2026-08-25

- 建立 `qifu-dailywork` 权威源码仓库。
- 修复照片编号占位冲突，可安全归位错误数字文件后补入正确照片。
- 修复单个 PDF 编号 OCR 离群导致的连续编号错误。
- 收敛照片 OCR 搜索空间，并增加 Windows OCR 严格编号裁剪回退。
- 改进供灯、供水场景图分类。
- 修复照片数和 PDF 页数性能计数重复累计。
- 增加真实目录只读规划回归、跨盘事务和场景分类测试。
