# 2026 年识别全局审计（进行中，非发布验收）

本轮针对用户授权的全年本地材料做离线复核。基线为 `d75442c` / `9.6.8-rc.2`，开发分支为 `codex/2026-recognition-audit`。未改生产订单、未上传照片、未部署候选代码；桌面默认入口不变。

## 已完成的覆盖与限制

- 盘点 2026-01-25 至 2026-09-07 的 226 个日期目录、5,377 张 JPEG/PNG 和 833 个 PDF。
- 5,377 张照片完成读取、哈希和匿名结构指标检查：解码错误 0，检查前后内容变化 0。
- 2026-02-26、2026-09-07 无照片；不能计为识别通过。未找到 1 月 1—24 日的日常目录；视频不在本次照片识别范围内。
- 大部分历史文件已经被处理、压缩、改名。因此回放隐藏旧编号文件名，以内容重新识别；这不等于复现全部原始相机文件。
- 历史数字文件名只作为待核对的参考，不是独立正确答案。结构检查有 318 个“历史福单/场景入口”不一致、453 个“历史场景/入口未通过”不一致。这是排查线索，不是最终错识别率。
- 全年完整 OCR、PDF 索引和照片/PDF 一一对应仍在分批复核。全量结构扫描不能代替这三个验收项目。

基线盲测（内容复制到本机临时目录；不调用 apply、浏览器或 runner）：

| 日期 | 照片 | 自动归类 | 未归类 | 与历史参考冲突 | 用时 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 2026-01-25 | 16 | 13 | 3 | 0 | 642 秒 |
| 2026-03-01 | 21 | 15 | 6 | 0 | 489 秒 |
| 2026-07-28 | 33 | 6 | 27 | 1 | 3,858 秒 |
| 2026-09-06 | 19 | 19 | 0 | 0 | 782 秒 |

1 月 25 日另有一个 PDF 编号在历史照片目录中本来就没有对应文件；不能把缺文件误报成 OCR 失败。新增定位模型试验在该日读到了原路径漏掉的 1 号，但严格的第二引擎验收仍有未决，尚不能替代原流程。

7 月 28 日冲突已经人工查看原图确认：纸面完整码为 `267-1-526`，旧 PDF 索引把对应页推为 `226`，形状匹配随后给照片提出 `226.jpg`。整日安全校验没有通过，因此只是错误计划，未执行改名。该盲测说明“PDF 索引错误 → 照片候选过滤 → 指纹跟随错误编号”的连锁故障真实存在。

中间候选 `e6b3d462c30c` 的整日盲测：

| 日期 | 照片 | 自动归类 | 未归类 | 与历史参考冲突 | 用时 | 未完成原因 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 2026-07-28 | 33 | 32 | 1 | 0 | 767 秒 | 30 张福单全部对应；另 1 张供灯场景被旧边缘密度阈值挡住 |
| 2026-09-03 | 15 | 14 | 1 | 0 | 682 秒 | 1 张纸面缺少可用指纹被旧逻辑当成硬冲突，整批暂停 |

第二项已改为“未决，不赋号；不阻断其他独立确认项”，并补充正反测试，最新实图回放在进行。第一项不能靠单独调大阈值仓促放行，因为全年结构检查也发现烛光背景福单容易被误判场景。

最终第一批候选 `cac1d48b2b6a`（工作副本字节指纹）再次完成上述两日回放：7 月 28 日仍为 32/33、0 参考冲突；9 月 3 日仍为 14/15、0 参考冲突，但 `safeToApply` 已从 false 变为 true，未决项不再变成整批硬冲突。最后一张并未被强制赋号。两次墙钟耗时分别为 939 秒和 4,335 秒；并行年度任务中也同时出现约一小时的耗时长尾，不能把这一轮拿来宣称算法提速或退化。

最新 PDF 专项 `588dbd93d0e8`：1 月 27 日、1 月 30 日、7 月 28 日、9 月 3 日共 59 页，未读编号、重复编号、历史照片编号不在 PDF 集合中的数量均为 0，共 201 秒。它验证 PDF 子模块，不代表全年或线上业务验收。

开发过程中一次本地标准字体路径的 Windows 分隔符错误使 4 个专项立即失败；该失败报告保留。修正后增加真正生成 PDF、调用渲染器及本地 OCR 的集成测试，防止纯函数测试漏掉此类资源问题。

上述早期基线回放进程加载的是起跑时的 JS 模块，但运行期间外部 Windows OCR 脚本增加了逐文件错误隔离。因此它是诊断证据，不是完全冻结所有依赖的正式 A/B 性能验收。后续工具使用完整识别源码指纹，正式验收必须在冻结快照中运行。

## 已定位的根因和本分支修正

| 风险 | 原设计的问题 | 本分支处理 | 验证方式 |
| --- | --- | --- | --- |
| PDF 基准错误 | 看不清的后续批次仅凭前段连续和页数补出尾号 | 禁止无锚点尾段猜号；禁止仅因剩一个缺号给整份未读 PDF 定号 | 不可读尾段、跨 PDF 缺口负例 |
| 年份当编号 | 重复的牌位年份也可能参与最优连续序列计算 | 带 OCR 观察的页面必须有业务前缀证据才能作序列锚点 | 三页重复年份负例 |
| 推断覆盖实读 | 序列优化可以重写页面上多框读出的完整编号 | 页面完整编号优先；保留真实导出缺口和四位尾号 | 有缺口的明确编号、四位尾号反例 |
| 月份/紧连编号 | 前缀限定三位；扩大到四位又可能贪婪吞掉三位前缀后的中间码 | 精确业务前缀优先，再做有边界的模糊解析 | 全年 12 个月 × 多种尾号/分隔符 |
| 文件名特例 | 某日仅凭一组普通文件名就套用人工编号 | 移除无哈希的人工赋号特例；原有哈希限定证据单独保留 | 防止特例回流的规则测试 |
| 纠错制造错号 | 已经完整的 90—94 仍被“离群修复”改成 95、91—94 | 已完整且唯一的连续编号集禁止再次挪动端点 | 实际历史反例的脱敏重放 |
| PDF 编号区留白 | 窄框内大量留白把字符压小，118/119 的中间码漏读 | 保留原路径，并增加仅针对编号行的去留白加边距读取 | 实际裁图、合成图像像素保全测试 |
| 备用证据污染 | WinRT 仅读出前缀/年份，也可覆盖原引擎观察并标 100% | 只接收完整业务码；保留原始观察，不伪造 WinRT 数值置信度 | 前缀、年份、歧义、空结果负例 |
| 陈旧编号缓存 | 同一天就复用 PDF 编号集，未核验 PDF 及识别规则版本 | 绑定业务日期、PDF 名称/内容哈希、识别源码指纹；过期索引重建，不删除上传回执 | 同名同大小替换、删 PDF、日期变化、规则升级 |
| 识别后文件变化 | 备份的是当前照片，但并未证明它就是之前识别的那张 | 计划绑定原图哈希，处理备份副本，移动前后再核验；回滚被占用时保留隔离原图 | 同名同大小替换、备份中替换、回滚占位模拟 |
| 单文件影响整批 | Windows OCR 一个文件异常就使整个子进程失败 | 每文件独立错误结果，保留其他成功结果并释放位图 | 有效—损坏—有效三文件实测 |
| 缺证据误报错号 | 无可用指纹或区分度不足，被标为已确认编号冲突 | 新提议保持未决、不赋号；只阻断真正冲突，其他确认项可继续 | 无指纹、低区分度与真实冲突正反例 |
| 独立证据被降级 | 两个引擎确认后的新结果仍继承旧提议单票数量，被场景或指纹兜底否决 | 明确识别独立双引擎完整码证据，保留其优先级 | 单票遗留值、烛光背景、重复同引擎反例 |
| 测试不隔离 | UI 测试加载真实设置并进入窗口消息循环 | 临时配置目录、禁止显示窗口、禁止真实初始化 | Windows UI 状态测试 |

PDF 窄编号区也加入现有本地便携识别引擎，读取原始完整编号证据；没有靠放开安全校验来“通过”。

没有内容哈希的其他历史批次捷径也已禁用，不能因为文件名和数量相同就视为人工已确认。跨盘测试脚本改为仅创建、清理自己新建的临时子目录，不再删除传入目录。回滚时若同步程序替换了刚创建的目标文件，也必须保留该文件，不能按原计划删除外部新版本。

## 核心设计仍需完成的重构

1. 文字定位和字符识别分开。现有便携模型主要是行识别器，位置兜底最多遍历 874 个窄框；移动拍摄构图后要付出很高的扫描成本。实验性文字检测器只在 `tests/experiments` 使用，未接入默认执行器。
2. 独立记录“读出了什么”和“是否属于本日 PDF”。不能先用可能错误的 PDF 编号集过滤掉原始识别证据，再把结果叫作“照片读不出来”。
3. 同引擎不同裁框不是两个独立引擎。实验报告同时保留两个引擎的观察，尚未确认的样本不能计为成功。
4. 场景判断不能只追逐全图亮度、暖色比例和日期特定阈值。需要前景纸张、编号、灯阵/供水结构的统一证据，并同时回放真实福单反例，避免把烛光前的纸张当场景。
5. 单张未决、引擎不可用、源文件缺失、PDF 基准冲突应是不同状态；确认过的独立任务可继续，未决项不能被错误完成。
6. 版本管理保留代码历史，但不能替代真实数据验收。旧测试曾把“猜出连续尾号”当正确结果；所以测试本身也必须接受业务规则审计。
7. 照片的三个 WinRT 严格兜底入口已统一为完整码多裁框观察，见下节；它不等于独立双引擎，也没有完成跨 PDF 修订的业务绑定。其他识别、序列与正文融合路径仍需逐项审计，不能据此声称核心重构全部完成。
8. 历史材料可能混有重新导出前后的两套编号。例如 4 月 14 日目录存在两组历史福单数字名，而当前 PDF 仅 14 页。差异必须区分旧副本、真实缺图、重新编号和识别错误，不能把所有集合差异都报成 OCR 错误。

### 新确认的核心风险：读对编号仍可能配错订单

2 月 17 日一张历史数字名为 446 的照片，纸面清楚印着 448。实验中的两个引擎均读到 448，起初它被统计为与历史文件名不一致。实际查看对应 PDF 后发现：照片正文与当前 PDF 的 446 页一致，而当前 PDF 的 448 页正文不同。因此这不是已证实的 OCR 错号，也不能直接把旧文件名改成 448。

这项实证表明材料存在打印编号与当前 PDF 编号的版本差异；仅靠本地现有文件无法还原差异发生的准确时间。本文不把它武断归因于某一次重新导出操作。原图及 PDF 没有被修改，线上也没有替换或上传。

由此增加发布阻断条件：**完整数字读对 + 当前 PDF 中存在同号，不足以证明订单归属**。当前生产代码以及本轮第一批候选仍有这类判定路径，因此候选不能发布为“根治版”。需按[编号与业务绑定重构设计](RECOGNITION-BINDING-DESIGN.md)区分纸面码、PDF 修订和业务绑定，并补充跨修订真实回放。不能因双引擎同意就跳过版本匹配。

### 正文匹配替代方案：已做否决性试验，未接入产品

隔离 TEMP 中安装固定 `opencv-python-headless==4.13.0.92`，参考 [OpenCV 特征匹配与单应变换文档](https://docs.opencv.org/4.13.0/d1/de0/tutorial_py_feature_homography.html)试验 SIFT、几何一致性与排除共用装饰后的正文笔画重合。它没有向外部发送图片，也没有改变执行器的依赖。

- 2 月 17 日上述跨编号案例：正文匹配倾向正确的当前 446 页；对齐后区分性笔画 F1 为 0.8739，错误的同号 448 页为 0.1283。
- 9 月 3 日的未决 32 号与四张供水 PDF 页面比较：简单特征匹配和笔画重合未把正确页面排在首位。颜色四边形方案也没有可靠取得纸张边界。实验文字定位模型另一次单引擎探测也未读到该图的完整码。

因此**这些替代方法也不能直接上线**。只验证一个成功样本就替换核心算法，会重演过去补丁反复的情况。本轮保留失败证据及实验代码，不把错误的最高分当成对应关系；这些分数不是正确概率，也没有成为任何自动赋号阈值。

## 当前验证和计时范围

### 9 月 7 日第二批候选检查点（尚未发布）

- 冻结轮次 `pdf-index-fa4923224a58` 完成 226/226 个日期、4,618 页，未读编号 6 页，重复编号 0，原文件哈希变化 0。6 页全部属于同一份额外打印清单；上一轮已经逐页渲染并人工查看，页面没有平台完整编码。该文件 SHA-256 为 `f6eebdf7d854588c68a1f9a02c218e3c6926a603f2fc4c60957bbc9759639a58`。这是资料类型问题，不应称作 6 次 OCR 失败；原始未读计数仍保留，不静默排除、不编造编号。其余 4,612 页读到编号也不等于照片业务归属已经验收。
- 实验性照片定位轮次在 21:28 的检查点完成 168 个日期、4,145 张照片；这是实验覆盖，不能合并为产品回放通过率。实验程序仍在运行，未另起重复全年任务。
- 另外两处历史文件名差异经原图和当前 PDF 正文人工核对：3 月 6 日旧数字名 1522，纸面与当前 PDF 都是 152；4 月 15 日旧数字名 33，纸面与当前 PDF 都是 333。它们与 2 月 17 日跨修订案例不同，不能把所有“文件名不一致”都统计成 OCR 错号。未按这些结果批量修改历史文件。

本批先复现再修复了以下问题：

1. 原照片 WinRT 路径先与当前 PDF 集合求交，导致两个框分别读到 32、33 时，只因 PDF 里没有 33 就把冲突删除。三个入口现在先汇总完整业务码、保留范围外的相反观察，再检查 PDF 范围。仅读到年份、前缀、末号或缺少中间字段不算完整码。
2. WinRT 不返回数值置信度，不能注入 100；新证据明确记录 `null` 和单一引擎身份，两个裁框不称为双引擎。重复编号修复也不能因 `null` 被数值转换成 0，就把这张完整码照片挪到剩余缺号。
3. 实图诊断复现 native 图片写出器在超过 300 字符路径下失败；改为同一编码器生成字节、Node 写文件，验证 PNG 字节一致与 JPEG 格式保持。WinRT 的路径读取另有长度限制，因此只为过长裁图创建短的本机临时别名，输出映射回原裁图身份，随后只清理本次新建目录。
4. 发布前追加的“有效长路径—缺失长路径—有效长路径”测试又复现别名准备中单文件异常导致整批中断。已补逐文件隔离；缺失项保留错误计数，其余观察继续返回。此问题在候选阶段发现，未进入部署版。

对应测试：`windows-code-evidence-regression.mjs`、`ocr-long-path-regression.mjs`、实际 WinRT `windows-ocr-long-path-integration.mjs`。完整 Node 与 Windows UI 回归通过过一轮；任何后续源码改动后仍必须重跑。DPAPI 桌面用户回环测试在当前模拟身份沙箱被跳过，不能写成通过。

严格 WinRT 四窄框路径对四个历史疑难样本的单独探测结果为 **0/4 自动确认**：引擎执行完成，均未读到可采信完整码，而不是引擎报错。这不是整个产品识别结果；它证明长路径修复不能被宣传为识别准确性已经解决。保留了各轮私有报告，后续需用冻结候选跑完整流程及留出日期。

仍未解决的发布阻断包括：纸面编号与当前 PDF 修订的业务绑定、远景供水福单、供灯场景和全年最终候选回放。此时不递增发行版本、不替换默认入口。

普通 Node 回归、Windows `ui/Run-Tests.ps1`（包括界面状态、进程安全和 4 个列表查询浏览器模拟）已通过；实际 WinRT 有效图片—损坏图片—非法路径—有效图片的逐文件隔离也通过。真实跨盘目录测试尚未运行，不能把跨盘模拟计为真实跨盘验收。

私有目录保存 `timing.json` 和 `timing-audit.json`，总任务保持未结束。此时 `photo_count=5377` 是完成读取/结构检查的素材数，不是识别通过数；默认单日 600 秒指标不适用于全年离线回放。逐日实际耗时以各冻结轮次报告为准，并发轮次耗时不可相加当作总墙钟耗时。早期计时阶段起点晚于总任务起点，不能当精确全链路性能基线。

第一批生产候选提交为 `d659ba8`。固定测试快照的 ZIP SHA-256 为 `5451258fd950fdd74c0c69d2a95bdd68423d224a6094c5f53a02d7ef86f19b2a`；从该快照继续全年度 PDF 复核，快照内源码指纹为 `fa4923224a58`。早期诊断 PDF 轮次在 106 个日期/2,235 页处停止，其证据保留，不和新候选合并计算通过率。

早期 GitHub 推送返回 `Empty reply from server`；9 月 7 日已通过正常配置成功推送至 `6e783ca`，此前各审计提交均已同步。没有关闭 TLS 校验或复制凭据。尚未创建 PR、合并主分支、打发布标签或部署软件。

### 9 月 7 日晚间：双引擎证据与真实正文复核

第二批冻结候选 `6e783ca` 的 ZIP SHA-256 为 `58ef1d4783efd08c41939a01cb509e310df3906c0be8a56035654fb31c7f99be`；识别源码指纹 `92ed23e7fe0f`。完整计划回放结果：

| 日期 | 照片 | 自动归类 | 未决 | 参考冲突 | 可执行已确认部分 | 用时 |
| --- | ---: | ---: | ---: | ---: | --- | ---: |
| 2026-09-03 | 15 | 14 | 1 | 0 | 是 | 470 秒 |
| 2026-09-06 | 19 | 19 | 0 | 0 | 是，完整计划 | 407 秒 |

两日原始文件哈希均未变化、均未命中人工赋号证据；这不是线上上传验收，也不是三次生产运行。9 月 3 日剩余纸张仍未解决。22:31 的年度实验检查点完成 215 个日期、5,192 张照片；累计 10 个历史参考差异，仍不能称为产品正确率。

进一步按完整 PDF 页面与照片正文人工核对，分流如下：

- 5 月 9 日旧文件名 43/57 对应纸面与当前 PDF 的 143/157；7 月 9 日旧名 144 对应纸面与当前 PDF 的 143；8 月 14 日旧名 319 对应纸面与当前 PDF 的 320。不能把这些文件名差异直接报成 OCR 错号。
- 5 月 2 日实验两引擎同意 40，但照片正文对应当前 PDF 48，当前 40 页明显不同；7 月 1 日两引擎同意 8，但照片正文对应当前 PDF 6，当前 8 页明显不同。6 月 13 日实验观察 289，正文对应当前 PDF 269，289 不在当前索引。这三例均不得按实验数字自动绑定。照片编码区域仅约几十个原始像素，放大不能恢复缺失笔画；仅凭这些模糊数字，不能进一步断言每例都是纯 OCR 误读而不是打印修订差异。已确认的是**数字共识与当前正文归属不一致**。
- 私有人工核对结果仅作审计标注，绑定原照片与 PDF 哈希，不作为产品硬编码赋号。未修改任何原文件或线上记录。

同一正文特征实验增加上述两组正反页面：5 月 2 日正确页的几何笔画 F1 仅 0.0587，而错误页为 0.4539；7 月 1 日颜色四边形对齐区分了两页，但 SIFT 几何匹配仍不稳定。其结果与此前远景供水反例一起说明：不能把简单 SIFT 或最高相似度直接升级成生产绑定证据。继续保留反例，不把实验排名叫作识别概率。

第三批候选先以脱敏测试复现了两个相邻漏洞，再修改实现：

1. 两引擎各两个框同读 68、第三引擎完整读到 66 时，旧 `independentCodeConsensus` 仍返回 68。现在任何有效的相反完整码都保留为未决；PDF 集合过滤只发生在观察共识之后。
2. 仅保留“双引擎”方法名、没有原始观察，旧复核仍可确认。现在必须由已记录的引擎、物理裁框及完整码支持所提编号；缺失或矛盾的证据不能走快速确认。

共用严格完整码解析器保留两处分隔符要求，不把 `269-132` 制造成 `269-1-32`，不把 `null` 前缀距离当作 0。有效证据不再继承旧提议的数值置信度，也不能因空得分被挪到剩余缺号。新增测试 `independent-code-provenance-regression.mjs`；全套 Node、Windows UI、实际长路径 WinRT 和列表浏览器回归已通过，DPAPI 两项仍为沙箱跳过。

**这两项程序修正不能解决所有“两引擎同读错误目标”案例。**正文与打印修订绑定层仍未完成；当前不能发布、不能把实验确认数当作可上传数。

### 第一轮全年实验完成与冻结回放检查点

9 月 7 日 22:59 读取完整检查点：`detection-c6a93e06dc7f` 已完成 **226/226 个日期、5,377 张照片**，报告级异常日期 0。实验原始计数为完整码共识 2,137、纸张未决 2,456、历史参考差异 11。这是单独实验路径，不能据此计算生产识别正确率；旧轮次没有逐日写出 `sourceUnchanged` 字段，也不能把缺字段报成原件全量复核通过。全年 PDF 专项已完成，最终生产候选全年计划回放仍未完成。

最后一处差异是 9 月 1 日旧文件名 14：原图完整码及当前 PDF 第一页正文均对应 1。现在 11 处参考差异均已有人工对照分流：7 处历史数字名与当前纸面及正文不一致，1 处清晰纸面码与当前正文所属编号不同，3 处实验数字与正文冲突且小字无法判明是字符误读还是打印修订差异。**这不代表 11 项都已在产品中修好，也不证明其他实验确认项全部正确。**人工核对记录仅存在私有审计目录，不进入生产赋号规则。

第三批代码候选为 `5d945a7`，已正常推送 GitHub。冻结 ZIP SHA-256：`2fff1e5b7292fe2177b3a67d61bba13234eeac12595c445bf3bf75189d34bb4c`；完整识别源码指纹：`d840dc94aa13786e8b4cf8cfbc69e4775b4b979a0cbd81057aed23fa0e608d1c`。正在 `replay-d840dc94aa13` 回放 5 月 2 日、7 月 1 日、8 月 14 日，共 67 张照片，尚未出整日结果。冻结副本不随文档更新变化。

仍须审计失败路径的证据持久化：独立冲突核查目前仅在成功确认时把新观察写回提议，失败观察与后续重复/序列兜底尚未形成完整不可变证据链。不能只改成功分支就声称所有矛盾观察都会阻止后续误赋号。回放 stderr 另出现极窄字符块的 Tesseract 警告；当前没有相应整日失败报告，须结合回放结果判断，不能直接当作照片损坏或忽略其性能影响。

### 9 月 7 日深夜：失败证据保留与单锚点分支复核

冻结候选 `5d945a7` 的三个整日回放已结束，原文件哈希均未变化、人工赋号命中均为 0：

| 日期 | 照片 | 自动归类 | 未决 | 历史参考差异 | 计划 safeToApply | 用时 |
| --- | ---: | ---: | ---: | ---: | --- | ---: |
| 2026-05-02 | 34 | 11 | 23 | 1 | false | 1,317 秒 |
| 2026-07-01 | 16 | 7 | 9 | 1 | true | 392 秒 |
| 2026-08-14 | 17 | 15 | 2 | 2 | true | 272 秒 |

**候选未通过验收。**7 月 1 日旧名 6 的照片仍提出 8，而先前完整正文核对已确认当前对应页为 6；它的 `safeToApply=true` 只表示现有程序校验放行，不代表内容归属正确。这是已知快速路径风险的真实重现。5 月 2 日另一张照片提出 55，但自身候选还包含精确前缀、高分单票 53；本轮不凭历史名 53 就断言正确答案。8 月 14 日第一张 319→320 已经正文核实，反向 320→319 尚需另查，不能把四个参考差异都称作新错配。

本批先复现失败，再追加以下修正（尚未发布）：

1. 快速 PDF 复核不能只看多数票，忽略照片自身仍有的强相反候选。该照片保持未决，不改名；这不是“根据旧名改回正确号”，也不是正文绑定已解决。
2. 独立复核成功、空结果、范围外结果、相反观察、引擎中途报错分别保存到独立的 `codeAuditHistory`。保存的观察和可变编号提议分开；后续修改方法名、给出缺号候选、重复/序列修复或重新划为场景，都不能抹掉失败记录。中途失败保留此前已返回的引擎结果；裁图清理失败不覆盖审计返回值。
3. 同模板“单个锚点扩展整段”原来漏掉其他序列分支已有的强 OCR 冲突检查。最小测试复现：66 的待补位置存在强候选 99，旧代码仍推为 66；现在整段保留未决，不跨冲突继续推断。

新增 `photo-code-audit-retention-regression.mjs` 包含上述负例、观察克隆/序列化保留、重复及缺号兜底、场景误分流、正确双引擎正例，以及真实异步收集器配合模拟引擎中途失败的测试。全套 Node、Windows UI、长路径 WinRT、4 项列表浏览器回归通过；DPAPI 两项仍为沙箱跳过。初次测试命令漏配依赖搜索路径导致加载失败，补上已配置运行库后才执行断言；该环境失败不计作业务缺陷。

这些修正保护的是失败和矛盾证据，不是完整的打印修订/正文绑定层。7 月 1 日的“所有观察同读错误目标”仍不能仅靠本批程序修改解决；最终候选历史回放、远景纸张和场景问题仍在发布阻断中。没有升级发行版本、改默认日期/登录方式或替换部署目录。

23:46 补做完整原件哈希复核：库存 226 个日期中的 5,377 张照片和 833 个 PDF，共 6,210 个文件全部与初始 SHA-256 一致，变化及不可读均为 0，用时 35.046 秒。本次逐项验证明确日期的直接父目录、拒绝符号链接并核验实际路径；没有扫描新增文件，因此只证明库存原件未变，不证明识别通过。第一次诊断把云同步占位文件的所有 `ReparsePoint` 一并拒绝，未执行任何哈希检查；核实并非符号链接后改用实际路径保护完成复查，失败与成功轮次均保留在私有报告中。

本批代码检查点为 `5375d83`；最后一次 Windows 全套回归包含新异步失败收集测试并通过。新的冻结快照 ZIP SHA-256 为 `42ee58d11b114b2c4b9b4850e64fee7526cd37896fd3bf105cd6418e843044ff`，识别源码指纹 `91a3c2aad61f1ab03e197505b8ab0f5e9c1642738a0277607010161d2c947a20`。私有目录 `candidate-5375d83-153030ea7e8b4288bcfabcf6e8ee9d33` 正在重放 5 月 2 日、7 月 1 日、8 月 14 日及 9 月 6 日，共 86 张照片；输出 `replay-91a3c2aad61f`，尚未验收。旧 `replay-d840dc94aa13` 已完成，不应重复启动。

### 9 月 8 日凌晨：候选回放完成与中文正文独立试验

冻结候选 `5375d83` / `replay-91a3c2aad61f` 的 86 张照片已经跑完，不再处于运行中：

| 日期 | 照片 | 自动归类 | 未决 | 历史参考差异 | safeToApply | 用时 |
| --- | ---: | ---: | ---: | ---: | --- | ---: |
| 2026-05-02 | 34 | 10 | 24 | 0 | true | 1,191 秒 |
| 2026-07-01 | 16 | 7 | 9 | 1 | true | 491 秒 |
| 2026-08-14 | 17 | 15 | 2 | 2 | true | 251 秒 |
| 2026-09-06 | 19 | 19 | 0 | 0 | true | 173 秒 |

四天原件哈希均未变化，人工赋号命中均为 0。5 月 2 日带强相反候选的 55 提议已变为未决，其他确认项可以继续；9 月 6 日完整计划仍通过。但是 7 月 1 日正文属于当前 6 页的照片仍被提出为 8，**因此这轮候选仍不合格**。单纯保护冲突历史不能补上缺失的正文绑定。

本轮检查了当前便携模型字典：436 项，不含汉字；它服务数字/英文编号路径，不能把该模型的存在称作中文正文复核能力。先以 Windows OCR 三个宽视图与 PDF 文字层比较，四张疑难样本中只有 2 月 17 日取得页面特有的正文片段；另外三张主要是空结果或共用模板，不具备区分性。读不到文字与引擎报错分开记录，不能混算。

随后仅在私有 TEMP 目录下载 [RapidAI 固定 v3.9.2 发布清单](https://github.com/RapidAI/RapidOCR/blob/v3.9.2/python/rapidocr/default_models.yaml) 中的 `ch_PP-OCRv4_rec_mobile.onnx`，SHA-256 校验为 `48fc40f24f6d2a207a2b1091d3437eb3cc3eb6b676dc3ef9c37384005483683b`，10,857,958 字节。模型内嵌字典 6,623 项，不能替换成英文模型字典。沿用隔离文字定位模型，新增 `tests/experiments/chinese-text-reader.mjs`；不改产品依赖、不向外发送照片、不改原 PDF。下载首次被当前网络/TLS上下文阻断，经正常授权后成功，未关闭 TLS 校验。

新增正文试验 `body-text-probe.mjs` 完全不向识别器提供历史数字名、目标编号或缺号清单。正文仅在内存比较，报告仅保留内容哈希、页码和计数；忽略数字码及各页共用模板，统计页面特有的连续汉字片段。输出始终标记 `diagnostic-only-not-binding`，不存在“最高分自动赋号”操作。两个裁框仍是同一个中文引擎的两个视图，不称为两个独立引擎。

第一组四张真实样本中，两种裁框的正文首选均与此前完整页面人工复核一致：

| 样本日期 | 当前 PDF 正文对应编号 | 两个视图的页面特有四字片段数 | 限制 |
| --- | ---: | --- | --- |
| 2026-02-17 | 446 | 33 / 33 | 纸面清晰码为 448，不能只按同号绑定 |
| 2026-05-02 | 48 | 16 / 18 | 旧数字路径提出 40 |
| 2026-07-01 | 6 | 33 / 19 | 旧数字路径提出 8 |
| 2026-09-03 | 32 | 1 / 2 | 证据很少，不能因第一名正确就放行 |

四张样本分别在当天所有 PDF 页中比较，而不是只比较人工指定的两个候选；原件哈希均未变化。这是独立正文路径的实验进展，不是生产修复、订单集合验证或全年验收。四字片段互相重叠，不是独立投票，计数也不是正确概率。

8 月 14 日和 9 月 6 日全部 36 张照片的整批对照已经结束，包含正常福单、场景、稀疏模板，不仅挑成功样本。这里的“稳定候选”仅表示同一引擎两种边距均取得唯一的特有文字首选且页身份相同，不是上线阈值：

| 日期 | 实际福单 | 正文稳定候选 | 福单缺少特有正文 | 场景图 | 场景产生特有正文候选 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 2026-08-14 | 14 | 12 | 2 | 3 | 0 |
| 2026-09-06 | 16 | 11 | 5 | 3 | 0 |

36 张全部复核原件哈希未变。9 月 6 日的 11 个正文候选均与历史参考及产品计划一致，但未逐张建立独立人工真值，因此不能写成“11 项订单绑定验收通过”。7 张福单仍缺少特有正文，其中包含竖版、稀疏模板；若把中文正文作为所有照片的强制通行条件，会使原来清晰数字可处理的照片停下来。6 张场景没有产生正文候选也只是本批观察，不是全年零误判证明。

01:32 按原图与完整 PDF 页面补核 8 月 14 日反向差异：旧名 320 的照片，纸面完整码及四个正文卡片均对应当前 319，当前 320 是另一组密集正文。结合此前旧名 319 对应当前 320 的核对，该对历史文件名确实互换，不能算作本候选新错配。只保存绑定照片/PDF 哈希的私有审计标注，不更改原文件，也不在产品中加入特例。

另补测 6 月 13 日既有正文冲突样本，不改试验算法及阈值，仍比较当日全部 45 页。中文两视图均选择此前人工核对的当前 269 页，特有四字片段为 12 / 8；原数字路径曾提出 289。该张原件哈希未变，耗时 7.710 秒。至此四个已知数字/当前正文冲突案例均取得与独立人工核对一致的正文候选；仍不代表打印修订/订单 ID 链已建立，不能据此自动改号或发布。

`body-text-evidence-regression.mjs` 覆盖数字与正文冲突、共用模板、同正文多页、空结果、页身份缺失/重复及报告不含正文；`chinese-reader-regression.mjs` 检查模型内嵌字典及损坏元数据。实际中文模型使用合成文字烟雾测试，正确返回完整测试句；不能用它代替真实照片验收。常规 Node 回归和 Windows 全套验证已结束并通过，包含新正文/字典测试、实际 PDF 渲染/OCR、长路径 WinRT 与四项列表浏览器回归。Windows 初次被脚本执行策略拦截，经授权仅本次测试进程使用执行许可后运行，没有修改系统策略。该桌面上下文中的两项合成 DPAPI 保存/读取/清除实际执行成功，没有输出跳过；不能据此宣称跨电脑凭据可迁移，也不改变手工验证码登录。

00:28 跟进的计时器在中断时未及时关闭，01:31 补关时明确标注包含中断与空闲，不能当作 40 张照片的计算耗时。本轮重新开始独立计时，全年总任务保持未完成。既有回放和正文对照均已产生完成报告，不重启它们；当前沙箱拒绝进程全量枚举，该检查失败保留，不绕过权限，也不以此推断存在仍在运行的任务。

### 9 月 8 日 03:29：扩大完整日期对照与竖排限制

从提交 `59e75ac` 冻结试验快照，ZIP SHA-256 为 `2906f1aed4b2570be2005376243b430c8e5c07796c3d55aa887731cdd3c85bec`，实际源码字节指纹为 `e67df805fa5b8c5758394de943ed232098ae152a9bac2639e51a4daeb588ddf2`。归档后的行尾字节与先前工作副本不同，分别记录，不能混称同一源码指纹。

`body-text-probe-7Po7rR` 完成 1 月 25 日、3 月 1 日、7 月 28 日全部 70 张照片，累计单图耗时 474.850 秒，原件哈希均未变化。61 张历史福单中 47 张有稳定正文诊断候选、14 张无特有正文；9 张历史场景均无特有正文。这仍是候选可用性，不是人工正确率或上线验收。

| 日期 | 历史福单 | 稳定正文候选 | 无特有正文福单 | 场景 |
| --- | ---: | ---: | ---: | ---: |
| 2026-01-25 | 14 | 4 | 10 | 2 |
| 2026-03-01 | 17 | 16 | 1 | 4 |
| 2026-07-28 | 30 | 27 | 3 | 3 |

1 月 25 日一张清晰竖排牌位的检测器实际提出了 8 个竖排块，但原中文试验仅读横排，两个中文视图都没有汉字四字片段；这不是原件损坏。独立方向试验中 90 度无有效片段，270 度得到 23 个当前某页特有片段。该结果只是竖排方向诊断，未以此自动改号。随后添加可选竖排读取、像素几何边距测试及固定模型合成文字读取测试；完整历史对照尚待新冻结快照运行。

合成整列文字的直接模型读取通过，但同一合成图经过检测器会被拆成单字框，未生成整列候选。这一失败限制保留，不能把单独行识别烟雾测试称作完整竖排流水线通过。真实图上的整列框与合成单字框都需要后续覆盖。

新增正文汇总反例先复现“两个视图第一名相同、另一个页面也有特有正文仍标稳定”，现独立标记多页证据；另覆盖横竖候选互相冲突、缺少视图、漏页/重复页等情况。上述代码只在试验目录和诊断汇总中生效，不更改生产赋号、登录、默认日期或桌面入口。

### 9 月 8 日 04:30：竖排整日对照完成、PDF 可见正文遗漏

提交 `1102774` 的冻结试验快照 ZIP SHA-256 为 `258aa2d22ec6af464a88ad58af14bd9d563ffdeb9a60cfe28b9474036ba48cdc`；正文试验源码字节指纹 `67bebadb1b9e66ab6255d5bbfa4b07b7f16c1d2a25475094ed37c27e6f947a92`。`body-text-probe-RpnSBP` 的 111 张照片已全部结束，累计单图耗时 760.855 秒，原件哈希均未变化。不应再显示为仍在运行。

| 日期 | 历史福单 | 稳定诊断候选 | 多页正文证据 | 无特有正文 | 历史场景 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 2026-01-25 | 14 | 7 | 7 | 0 | 2 |
| 2026-03-01 | 17 | 16 | 1 | 0 | 4 |
| 2026-07-28 | 30 | 29 | 1 | 0 | 3 |
| 2026-08-14 | 14 | 13 | 0 | 1 | 3 |
| 2026-09-06 | 16 | 11 | 4 | 1 | 3 |

另外五张已知疑难样本均保留此前的稳定正文候选。全轮共 81 个稳定诊断候选、13 个多页证据、17 个无特有正文，其中 15 张历史场景全为无特有正文。没有逐张独立人工真值，不能将这些计数写为准确率、自动改名通过率或正式验收通过。

上轮完整 Windows 测试实际执行成功，包含两项合成 DPAPI 回环、长路径 WinRT、PDF 渲染/OCR及原有回归，没有把沙箱跳过计为通过。提交 `1102774` 的两次正常 GitHub 推送分别遇连接重置、空响应；截至本检查点仍待推送，未绕过网络或权限设置。

进一步完整页面查看和区域诊断确认：1 月 25 日某黄色牌位上固定模板文字，在对应 PDF 的可见页面中存在，却不在文字层中；同一句话在普通福单页文字层中存在。照片左侧固定文案被误算为普通页的 5 个特有片段，右侧变量正文另给对应牌位页 3 个特有片段。前者分数更高也不能作为正确目标，新增多页检查已使其保持未决。

该牌位 PDF 页的文字层仅 13 字符；完整渲染的中文竖排两视图实际读到了缺失的固定文案。这证明 PDF 侧提取不完整，而非照片必然模糊。曾尝试查看私有提取位图时遇文件权限拒绝，该位图本身未完成查看且没有绕过权限；上述结论来自获准读取的完整 PDF 页面与文字层对照，不冒称嵌入位图 OCR 验证通过。

先用脱敏反例复现了“同一模板一句为可提取文字、另一页仅有图像时出现虚假的页面独有片段”（旧结果 3，正确应为 0）。新增试验把 PDF 文字层和每页完整渲染 OCR 的片段集合合并，分别保持来源边界，检查完整页集和四个视图。它只改变离线诊断，后续须从冻结快照做真实整日正反回放；不改产品编号、原 PDF、部署版或登录日期设置。

03:29 跟进计时在用户提问中断时未及时收尾，本轮补关并明确标记含中断/空闲，不能当计算耗时。两轮试验累计 181 次照片读取、111 张不同照片；不是 181 张独立样本。年度总任务仍未结束。

### 9 月 8 日 05:20：PDF 可见正文受控对照与短字段限制

从 `0af0cdd` 冻结的诊断快照 ZIP SHA-256 为 `5f720a8501f9333d59d817e04457fd9101bf75ab14b56c7720c4f68e109a9471`，实际源码指纹 `799bcaf9ff0fbd1e89f3f38b093798beffbc77a80922abc04cacff7ba69505f2`。每页完整渲染后以固定中文模型读横排、竖排各两个边距；每张照片只读一次，同一组读数分别对照 PDF 文字层及补充可见文字后的语料，因此不是两轮不同照片读数间的比较。所有语料来源分别取片段集合，不跨来源拼接新句子。

五个完整日期、106 张照片、94 页 PDF 已完成受控对照；本轮全部原件哈希未变：

| 日期 | 照片 | PDF 页 | 仅文字层稳定候选 | 补充可见文字后稳定候选 | 补充后仍未决福单 | 历史场景 | 用时（秒） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2026-01-25 | 16 | 15 | 7 | 13 | 1 | 2 | 77.980 |
| 2026-03-01 | 21 | 17 | 16 | 17 | 0 | 4 | 194.961 |
| 2026-07-28 | 33 | 30 | 29 | 30 | 0 | 3 | 335.301 |
| 2026-08-14 | 17 | 16 | 13 | 13 | 1 | 3 | 141.450 |
| 2026-09-06 | 19 | 16 | 11 | 15 | 1 | 3 | 139.454 |

91 张历史福单中，稳定诊断候选由 76 增至 88；另 15 张历史场景仍无特有正文。剩余福单为一张两视图不一致和两张无特有四字片段。没有新增历史参考差异；8 月 14 日既有的 319/320 互换参考已由此前独立页面查看确认。历史数字文件名并非真值，这些数量不能换算成准确率或订单绑定通过率，也没有用于自动赋号。五轮用时合计 889.146 秒，其中 PDF 可见文字读取累计 481.782 秒；这是离线实验成本，不是正常单日业务性能。

对 1 月 25 日此前固定模板造成的多页反例，补充 PDF 可见文字后，对应黄色牌位两竖排视图均仅剩正确页的 5 个特有片段，错误普通页不再含特有片段。另完整查看了 3 月 1 日新恢复的一张照片与对应 PDF 全页：完整编码、中央对象及左右字段相符。仅保存照片/PDF 哈希绑定的私有人工审计证据，不把人工目标塞入识别器，也不宣称已验证线上订单 ID。

两张无特有正文福单另做了独立短字段诊断：8 月 14 日三卡福单及 9 月 6 日单卡福单中的三字字段，实际上均被两个横排视图读到，默认四字片段比较却没有计入。因此不能把这两项笼统归为 OCR 没读到。只记录字段长度、命中布尔值及同日 PDF 文字层完整字段重复数；该重复数不是完整可见正文的子串唯一性，也不能排除重复订单。下一步应验证字段位置、内容覆盖和模板排除，禁止简单把所有片段阈值降为三字后自动绑定。

诊断汇总也补了失败优先回归：原来两个视图候选页数相等但非首选页身份不同，仍可能被算为稳定；`a43e076` 现要求精确页身份集合一致。新增 A/B 汇总从逐照片证据重算，不信任自报聚合数量，缺照片、漏页、重复 ID、日期/模型/源码混用均拒绝计分。两个缺陷均先复现失败再修正。完整 Node 及 Windows `ui/Run-Tests.ps1` 已实际运行通过，含两项合成 DPAPI、真实 PDF 渲染/OCR、长路径 WinRT 和原有浏览器列表回归，无 DPAPI 跳过。

此前推送失败已恢复：`1102774`、`0af0cdd`、`a43e076` 均已通过正常 Git 配置推送到审计分支，没有关闭 TLS 或改用其他凭据。此后产品 `src`、`ui`、依赖和版本相对 `5375d83` 未改；7 月 1 日产品的 6→8 绑定问题仍是发布阻断。五张已知疑难样本正在使用同一冻结诊断快照，逐日期读取全部 PDF 页后再对照；不得把先完成的五个正常日期冒称为这五个疑难样本也全部结束。

05:21 保存检查点时，五张疑难对照中的前四张已完成：2 月 17 日在全部 103 页中仍选 446（33/33 特有片段）、5 月 2 日在 30 页中选 48（16/18）、7 月 1 日在 13 页中选 6（33/19）、9 月 3 日在 12 页中选 32（1/2）。后者证据仍很少；这些均只是诊断候选。6 月 13 日的 45 页对照仍在原进程运行，未重复启动。`a43e076` 的新汇总器已从逐图报告重算九个完成轮次（110 张照片、252 页），均通过报告完整性检查，另一个进行中轮次明确不计分。

05:24 最后一组已结束：6 月 13 日的 45 页对照仍选此前人工核对的 269（12/8 特有片段），用时 272.826 秒，原件哈希未变。原任务正常退出；十轮全部重新通过精确页集与逐图报告校验，共 111 张照片、297 页 PDF、93 个稳定诊断候选、1 个视图不一致、17 个无特有正文。各轮耗时合计 2,193.137 秒；所有五张已知疑难样本保持已有正文候选，但仍不具备生产订单绑定或全年验收结论。不得重复启动这一轮；后续应处理字段级证据与产品集成门槛。

自动跟进已启用且包含验收后的 Git 同步、可迁移包和主动通知。任务执行完毕不等于验收通过；仍有错配、缺证据或待确认权限时不发布正式版。默认桌面入口仍必须等连续三次真实业务稳定运行；本轮没有线上写入、发行标签或部署替换。

### 9 月 8 日 06:31—07:49：提交时 PDF 变更保护与字段来源检查

`fb2070f` 修复产品提交阶段的一处独立安全缺口：原实现只在 JPEG 暂存前核验 PDF，暂存/备份期间 PDF 同名同大小被换掉后仍可能提交旧计划。新增六种隔离事务回归，先复现旧代码未拒绝备份后替换，再在提交前、目标写出后重复核验 PDF 文件集合与内容哈希。变化时停止，按已有哈希保护回滚本次目标并恢复原图/重复来源；不回滚外部修改的 PDF，也不宣称取得了 NAS 原子锁。这是代码审计与模拟复现，不是已证实造成过往照片 OCR 故障的原因。

`4f7109b` 保留字段诊断的换行边界，避免跨两行制造完整短字段。真实中文读取器本来以标点分隔检测行，因此不能声称这个边界修复解释了已有实图失败。冻结快照 ZIP SHA-256 `d9583672ee468da07c575603af14c4016acb3874dd980c07f934d987a4803eea`、诊断指纹 `b5a6f646e2ec6635e4ad982c8aa65ccef146a8903b18f13281a772b5cea56ae0` 的五轮已完成：4 月 20 日全部 19 张、8 月 14 日和 9 月 6 日各一张短字段、7 月 1 日和 9 月 3 日各一张已知疑难，共 **23 张 / 72 页 / 447.596 秒**。原件哈希均未变；7 月 1 日仍取得正文对应当前 6 页的诊断候选，并不表示产品的 6→8 问题已修复。

4 月 20 日有 15 张历史福单、4 张场景：四字片段及字段诊断各有 13 个稳定候选，但不是同样 13 张。完整查看照片和 PDF 后确认，一张竖排牌位已经有 22/21 个特有四字片段，字段诊断却为零：PDF 的可提取内容被拆成逐个汉字，没有长度大于一的完整文字项。另一张稀疏横版纸张的变量行目视与 PDF 一致，但两个边距读数不一致。不能把这两类统称为没有识别出照片，更不能用历史数字名做自动纠正。

`5f16921` 新增可见 PDF 整行字段来源，要求同一横排或竖排布局的两个视图确实读到同一完整行；字段保留提取来源/可见来源计数。`a5b6c1f` 同时阻止四字片段跨 OCR 换行或不同 PDF 文字项拼接。既不按 PDF 存储顺序组装竖排姓名，也不拼接不同来源；共用模板、姓名为他页长字段子串、单视图和来源不完整仍不产生单页字段候选。全部是离线诊断改进，未接入产品赋号。

新增反例先实际失败后修正。Node 全套和 Windows UI、实际 PDF 渲染/OCR、长路径 WinRT、四项列表浏览器回归通过。Windows 普通沙箱两项 DPAPI 被跳过；随后在获准桌面上下文单独运行 `process-safety-tests.ps1`，两项合成 DPAPI 保存/读取/清除实际通过，无跳过。不读取真实凭据、不改登录方式。

新的 `a5b6c1f` 冻结 ZIP SHA-256 `ad025779783c480d33ae7510ef09c7020fe5216bf97463cd61d8f4076568c2bb` 对照四个完整日期及两张疑难，共 73 张 / 87 页，已于 08:00 全部结束。产品识别源码指纹为 `128b2392ee5f695a49a7184883dc955f3d75951f09a620bf809c956d4894a3d1`，诊断源码指纹为 `6e0258299fdc0bad68046b09ed271a1d98c00e0a570e407e5af1288b2e146b5f`。六轮耗时合计 825.390 秒，原件哈希均未变。字段结果为 59 个单页候选、12 个无特有字段（全部为历史场景）、2 个视图不一致；四字片段对照则为 56 个稳定候选、15 个无特有正文、2 个视图不一致。两类计数不能混为准确率。

4 月 20 日字段候选从 13 增到 14，恢复上述竖排牌位；4 张场景无字段候选，稀疏纸张仍未决。1 月 25 日、8 月 14 日、9 月 6 日分别取得 14、14、16 个字段候选；7 月 1 日一张仍对应当前 6 页，9 月 3 日一张字段仍不一致。逐图核对日期、模型、源码、四视图完整页身份集合，并从逐图排名独立重算字段状态，六份自报汇总与重算一致；候选的历史参考差异只有已人工复核的 8 月 14 日 319/320 互换。文字层单独对照如实标记逐字提取缺少完整片段，不再用跨项拼接制造的片段声称语料完整。上述仍是诊断，不是正文/订单完整绑定或产品修复。

上述四个提交本机已保存；本轮正常 GitHub 推送遇 `Connection was reset`，尚未同步，不关闭 TLS 或改用其他凭据。06:31 计时在中断后补关，墙钟包含空闲，不能作为 23 张照片的主动计算耗时。当前没有发布标签、PR 合并、部署替换或线上写入。

### 9 月 8 日 08:33：场景补图容量不能替代类别证据

生产代码审计确认另一项旧缺陷，已用生成图片先复现失败：两张供灯占满 `2.1/2.2` 后，新增的明确供灯图被直接分配为 `2.5.jpg` 供水；反方向也有同类风险。类别不明的单图，以及已验证两张供灯后的独立补图分支，也会按空位而非画面分配。原测试甚至把“灰色未知图片 + 供灯位置已满 → 供水”当作成功，因此单纯保留旧测试并不能防止这项错误延续。

`cf6930f` 移除上述按空位推断和无独立类别证据的相对亮度拆分。先按原有单图视觉规则分类，再检查对应类别的可用位置；超过容量的那一类保留待确认，不挑两张凑数，也不改成另一类。其他独立明确且容量足够的类别可以继续，单张清晰供灯/供水补图保持可处理。已验证批次也走同一分类入口，不能仅因已有两灯就断定新图为水。

新增真实评分的 13 组生成图像回归（满位反向误分、未知图、相对明暗、三灯一水、明确/未知混合、单张和同类补图），旧代码第一组实际失败，新代码全通过。Node 全套与 Windows UI、实际 PDF 渲染/OCR、长路径 WinRT、四项列表浏览器回归通过。普通沙箱两项 DPAPI 跳过后，获准桌面上下文另行实测两项合成凭据回环通过，无跳过；未接触真实凭据。环境未提供 `npm.cmd`，使用 `package.json` 的同一测试入口 `node tests/run-tests.mjs` 实际运行，不把找不到命令的第一次尝试计为通过。

冻结快照 ZIP SHA-256 为 `e9c8f423dc4084a65686e64f356fafd7616a552b8242f2cbc8703a4d53182135`，产品源码指纹 `35d615bccb4b6637f9e927b2ca891348a0aad0494e7be6e7431130e69f858204`。`scene-category-replay-NAMmFP` 已完成 224 个有场景的日期、767 张历史场景、2,525 组条件对照，用时 606.760 秒，原件哈希均未变。逐场景重算容量不改变类别的约束：旧代码在模拟满位条件下 472 次改变了单图类别、590 次给未知单图猜了类别；新代码均为 0。这是同一照片多条件的模拟次数，不是历史生产误上传次数。

整批空位对照中，345 张保持原类别、1 张从旧供水纠正为供灯、99 张原来被整批挡住的明确项可以继续、145 张原先靠相对明暗或容量假设分配的图片转为未决，另 177 张仍未决。新代码整批分配 445 张，并不等于 445 张视觉真值全部正确；295 张单图仍缺独立类别证据，另有同类超容量限制。需要继续改进视觉识别，不能为了降低未决数恢复按空位猜测。场景专项不测试福单/场景入口，也不能代替完整订单验收。

完整查看两个实图：1 月 25 日一张多层灯阵，在模拟两灯位置已满时旧代码提出供水，新代码按容量保留；7 月 10 日一张明显有火焰的灯阵，旧整批明暗聚类覆盖单图供灯证据而提出供水，新代码保持供灯。私有标注绑定源照片哈希，原图未改。这证明真实图片能复现相应路径，不证明当年生产真的运行过相同满位条件。

同一冻结候选 `replay-35d615bccb4b` 的两日完整计划也已结束：7 月 1 日 16 张中分配 7、未决 9、历史参考差异 1，用时 521 秒；9 月 6 日 19/19 全部归类、差异 0，用时 196 秒。两日原件哈希未变，人工证据命中均为 0。7 月 1 日已知正文属于当前 6 页的照片仍提出 8，说明场景修复没有解决核心跨修订绑定风险，候选继续不合格。没有修改冻结副本、默认入口、发行版本或线上状态。

本轮末尾工具进程启动出现约 80 分钟异常延迟及 `spawn_ready` 超时，恢复后核实既有任务均已退出；不把这段工具/环境等待计作 OCR 性能，也不重复启动已完成的全年场景专项。正常沙箱 Git 推送遇 `schannel SEC_E_NO_CREDENTIALS`，本机提交保留，后续仅使用正常授权桌面上下文同步，不关闭 TLS 校验。

## 发布门槛（尚未满足）

- 原有与新增单元测试、隔离 Windows 回归均通过；不把跳过的测试写成通过。
- 全部可用历史照片完成冷启动回放，记录未决、错配、缺原件、人工证据命中及耗时，逐项解释。
- 修正前后的结果按同一输入哈希、同一规则/模型快照比较；新错配必须为 0。不能借放宽判断来降低未决数量。
- 对明显失败样本加入脱敏回归及独立留出验证；新相机角度、纸色、月份、编号长度和增量补图也应覆盖。
- 同纸面码在不同 PDF 修订中属于不同正文、旧纸面码对应新 PDF 编号、以及迁移后缺少旧回执的情况必须明确分流；不得只凭数字同号自动关联。
- 不以离线识别通过宣称线上上传、回执和闭环已验收。真实业务连续三次稳定后才考虑更新默认桌面入口。

不能保证未来所有照片都可自动识别，更不能承诺永远零 bug。目标是减少未决、禁止错配静默通过，让每个故障有证据、有回归、能追溯且可安全恢复。

## 实验模型来源

仅下载公开模型，不向网络发送业务材料。文字检测模型固定使用 [RapidAI 发布清单](https://github.com/RapidAI/RapidOCR/blob/v3.9.2/python/rapidocr/default_models.yaml) 中的 PP-OCRv4 mobile，SHA-256 为 `d2a7720d45a54257208b1e13e36a8479894cb74155a5efe29462512d42f49da9`。预处理参考 [PaddleOCR 2.7 检测流程](https://github.com/PaddlePaddle/PaddleOCR/blob/release/2.7/tools/infer/predict_det.py)。实验的连通域候选提取不是官方多边形后处理的等价实现；必须实测，不因引入模型就宣布可靠。
