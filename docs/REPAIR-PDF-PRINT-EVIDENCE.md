# PDF 完整码观察保留（未发布）

2026-09-10，工作分支 `codex/pdf-literal-evidence-20260910`。

## 已确认的缺陷

`indexPdfCodes` 的现有候选并非完整 OCR 观察：Paddle 先经过分数及预期前缀筛选，全部候选再排序后截为前八项；Windows 补查也在写入前限定前缀和可接受尾号。后续审计无法据此证明没有低分或跨月份相反观察。打印月份来自后端导出时刻，并不一定等于照片业务月份；本轮不据此猜测哪份历史订单发生跨月。

## 修改范围

- 增加独立 `printCodeEvidence`，保存每次本地编号读取的规范化完整码及空白/不完整尾号/失败标记，无预期业务前缀入参，无候选排序或数量截断。
- 记录绑定实际读入的 PDF SHA-256、物理页、实际 OCR 输入 PNG SHA-256、引擎、布局、原始数值置信度和量纲。可用的 Paddle 模型哈希一并保存；记录校验和仅检测变化，不是签名或 OCR 正确性证明。
- Windows 保留空读及错误结果通道；未返回结果不伪造成功。Paddle 原始/去空白视图分别记录，单个视图失败不吞掉其他视图。Tesseract 异常仍中止，本地留下 `complete:false` 的私有部分记录，稀疏模式在退出前恢复。
- 新通道不保存客户正文、原始 OCR 全文或异常原文；完整码仍属本机业务证据，不进入 Git。私有计划的旧候选选择、业务日期、上传月份和上传权限没有被新通道替换。
- 新模块纳入识别源码指纹，旧缓存不能冒充新版观察。未实现可信逐页订单绑定、混合打印月份上传或全部场景识别。

## 本轮实际验证

- 先添加回归：缺少新模块时测试失败。接入实现后新增 7 项通过。
- 实际渲染 + 可控 OCR 的入口测试：13 个完整码全部保留，而旧候选仍截为 8；包括不同前缀相同尾号。另测读取失败的部分记录、模式恢复、Windows 在候选否决前保留相反前缀。
- 普通 Node 全套 97/97，退出码 0；真实 Tesseract/Paddle 合成 PDF 两页联测也检查了新记录及源文件/物理页身份。
- Windows 完整测试在加载 `ui/Run-Tests.ps1` 时被脚本执行策略拒绝，退出码 1，未运行测试主体。未绕过或修改策略，未计为通过。本轮不推送或发布；等授权解决测试执行门槛。
- 历史 PDF 新旧入口对照未开始。不能以这些合成回归宣称新增识别成功、历史订单正确或年度验收通过。

## 后续验收

先完成 Windows 全套，再冻结源码做历史 PDF 编号结果和完整观察通道的逐页对照；核对原件、裁图、完整码与失败保留。之后才能据此设计打印命名空间与业务日期分离，并完成真实订单绑定。已知场景反例及全年新候选验收依然是独立发布门槛。

## 21:32 局部只读验证补充（不替代 Windows 完整测试）

Windows 授权仍未收到。为核验独立保留通道，运行明确拒绝全部子进程的局部 PDF 索引试验；没有执行被阻止的脚本或修改策略。实际子进程请求为 0，所选两日期无需 Windows OCR 分支。未进行照片重命名、NAS 写入、浏览器登录、上传或订单状态变更。

- Git 快照：`6967e09a30fdcf759c5f93f0dcf588a617a48a5f`；归档 SHA-256 `f622f317a8dbacd322f29c1fd081fd09c0a55486a4587f835d4a58448c52d8b5`。
- 3 月 3 日：四份 PDF、69 页、276 项观察；69 个尾号与此前冻结计划一致。18 页存在相反完整码；其他前缀 0 页。
- 5 月 2 日：四份 PDF、30 页、128 项观察；30 个尾号与此前冻结计划一致。26 页存在相反完整码；其他前缀 25 页。早期抽查显示不少来自低分宽区域 Tesseract 读取，不能据此推断真实月份被改写。
- 独立核验新通道的 202 份 Tesseract 实际返回记录，并重构/重读 202 份 Paddle 输入；全部裁图哈希、模型哈希、完整码和置信度相同，原 PDF 哈希不变。这里只证明保留通道及确定性，不证明 OCR 答案正确。

额外检查去空白整段读取是否能增强弱第二引擎：仅选择 Paddle 原始/去空白两个视图均达原有 0.85 阈值、完整码一致、Tesseract 原行弱/无完整码且没有强相反 Tesseract 的条目；不输入预期月份或尾号。固定单词模式一次补读，使用原去空白输入，原始观察不删除。85 条中 61 条与 Paddle 同码且达 50 分，24 条未确认；相反完整码 9 条，其中 4 条也达 50 分。该方案不能直接替换或被当作解决了 61 个业务错误。

后续计划对可见原始位图弱读数做固定补读，但创建实验脚本时工具审批服务返回容量错误并拒绝，脚本未创建、未运行；没有用另一路径重试。当前两个已执行进程均已结束，最新全年回放仍未开始。完整 Windows 回归仍待明确授权，故本分支继续只保留本地提交、不推送、不发布。

## 21:57 后续：结构校验修复（独立分支、未发布）

从 `463c0ea` 建立 `codex/pdf-evidence-integrity-20260910`。新观察通道之前的验证只核对来源及摘要，不能排除内部字段不一致且已重算摘要的记录；追加接口也没有完整检查可选 `sourceCropSha256` 与引擎量纲。该缺陷由合成反例确认，尚无证据说明历史照片失败由它造成。

现在逐项核对固定结构、连续序号、支持的引擎／状态、实际裁图与来源裁图哈希、模型哈希、引擎置信度量纲、完整码／前缀／末号内部一致性及同次读数唯一性。失败或跳过不允许携带成功编号或分数；Windows OCR 保持无数值分数；无效追加不会先改写原记录。稀疏数组、空记录和循环结构会被拒绝而非抛出非预期验证异常。不按预期日期过滤 OCR，前缀看似无效及末号含前导零的语法完整观察仍保留。

四项新增回归，其中三项在实现前失败；实现后普通 Node 101 项通过，含实际 PDF 渲染入口及双引擎合成页联测。进一步用原 `6967e09` 局部实测保存的 99 页／404 项记录验证兼容性，全部通过、记录及原 PDF 未变。这不是重新执行 OCR 或年度验收。校验仍不是签名，不能防止重新构造语法正确但假的 OCR 内容，更不代表订单绑定或上传许可。

原始位图补读的审批服务问题在本轮按同一路径重新提交后获准；与 Windows 脚本执行策略门槛分开。首次启动在任何 OCR 前发现提取器源码字节哈希不同，独立确认只有 CRLF/LF 差异，分别固定两份字节哈希与规范化内容一致性后重启。99 页身份全部核对，21 项符合预先设定条件的弱读获得一次 PSM 8 补读，新增高分同码 0、相反完整码 0、仍未确认 21，耗时 33.649 秒。原来的 25 项同码弱读有四项不符合本次入口，并未被算作成功。后续独立重新提取核对八份原 PDF、99 个物理页及全部 21 份实际输入哈希，未再次执行 OCR。新试验结果 SHA-256 `0b9d765105c3636ce6c37b7896bb12a06d55adc4c6ed4d2abbddd60091417402`。

该补读没有改善足够可靠的确认数，未接入产品。当前修改只修补新证据通道的结构保证，不宣称提升历史识别率。完整 Windows 测试仍待明确授权；不改变系统策略，不推送、不改版本、不部署，不把旧全套通过记录用于新分支。
