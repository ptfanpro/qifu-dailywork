# 开发与发布规则

- 本仓库是日常祈福执行器唯一权威源码；不再以复制整套版本目录作为开发方式。
- `main` 只接收已经通过测试的版本。每次修改使用独立分支和 Pull Request。
- 每个生产故障必须先形成脱敏回归样本或最小模拟测试，再修改实现。
- 推送前运行 `npm test` 和 Windows 端 `ui/Run-Tests.ps1`。
- 上传、状态变更和历史补录必须保持幂等；回执以业务日期、文件名、SHA-256 和订单 ID 哈希为依据。
- 照片、PDF、订单正文、账号、Cookie、浏览器配置、运行断点和 NAS 临时文件禁止提交。
- 版本通过全部历史回归后才更新 `package.json` 和 `CHANGELOG.md`；连续三次生产稳定运行后再更新默认桌面入口。

## 统一发布检查（不可只运行普通测试）

在 Windows 上运行 `npm run test:release`（或 `node tests/release-gates.mjs`）。入口先运行已知场景语义反例，再运行完整 Windows 测试；任一非零退出、启动错误、超时或非 Windows 环境均判失败。脚本不会改变机器执行策略、创建版本标签或部署。

`npm test` 仍用于开发回归，其通过不代表可发布：场景语义反例当前包含尚未修复的真实缺陷，必须修复对应识别能力，不能删除测试、改成跳过或仅靠安全拦截使检查变绿。新增已确认缺陷也必须纳入该发布入口。

即使统一代码检查通过，仍须独立完成同一冻结源码的全历史回放、原始材料完整性、照片与逐页订单归属及授权线上验收。检查结果中的 `releaseAccepted` 固定为 `false`，避免把代码测试冒充业务验收。未满足这些门槛，不得升级版本号、打发布标签、替换固定入口或声明问题已全部修复。
