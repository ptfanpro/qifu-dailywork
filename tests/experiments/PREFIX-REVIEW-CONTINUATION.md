# 前缀核验的续跑与残余版式筛选回归

本次不调整识别门槛、改写原始读数或增加自动赋号权限。两处缺陷均先用测试在
修复前复现：缺少先前模型报告时抛出 `ERR_INVALID_ARG_TYPE`；已充分核验的
前缀/正文候选在残余选择中丢掉前缀报告，仍进入版式处理（生成夹具中表现为
`positioned-layout-unavailable`）。

修复：

- 当前完整前缀链缺失原始读数、先前报告或有效运行时材料时，安全返回无证据，
  不再在 JSON 摘要计算处中断整个计划。复制或反序列化的报告仍不能充当现场证据。
- 残余版式选择传递与最终核验完全相同的 `prefixCodeReview`。充分的前缀/正文
  条件已经成立时跳过版式；未充分支持的候选仍走原来的受保护路径。

## 已执行的验证

- `prefix-code-review-regression.mjs` 增加 4 种缺失/失效先前报告、2 种缺失原始
  读数及实际正文收集器回归；旧 18 类拒绝、来源修改、模型篡改等断言保留。
- 普通 235 项及导入断言通过。Windows UI、进程、长路径 OCR、固定模型、
  缓存和本地浏览器测试通过；两项沙箱跳过的 DPAPI 已在获准正常桌面上下文
  用虚构数据补测通过，不读取实际凭据。
- 带明确便携运行时的版式集成：1 个联合判定、11 类拒绝、4 次重启和 24 个
  位置测试通过。首次命令缺少运行时参数的失败不计为通过。
- 真实单图收集器：复用并保留原双引擎观察，新读 4 个模型裁剪、18 页 PDF 和
  1 张照片正文（19 次新正文读取，零缓存）。正确的既有编号保持，版式状态
  为 `not-needed`；禁止的子进程/网络调用为 0，原件哈希不变，142.787 秒。
  这是定向收集器验证，不是 21 张整日重跑或新的识别成功数；并行计算耗时
  不能直接作为日常速度提升比例。

冻结生产源码指纹：
`ccd8046cd3250e488d49a49579419dc3a449c8ab082b95b5bd5dc5dd2012a545`。

## 跨日期回放边界

此前的完整历史轮次包含 226 个日期记录、5377 张、4010 个分配和1367 个未决；
它属于旧源码，不能冒充当前修复的全年通过率。本轮在父提交冻结版本上实际
重跑一批早期日期的全部 7 张 / 5 页：6 个分配、1 个未决、零历史参考差异，
163.641 秒。未决的原始/补充模型仍有前缀异读，未绕过。

48 张高未决整日的父提交回放与 16 张整日的本修订回放另有独立进度和结果，
不与本定向验证合成通过率。原图、PDF、OCR 正文、映射、运行进度和本机证据
路径均只保存于本机专用私有目录，不入仓库。完整全年、跨电脑及线上业务
验收尚未完成，仍为候选版；未修改原业务数据、固定入口、默认日期或手动验证码。
